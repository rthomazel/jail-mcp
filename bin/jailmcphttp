#!/bin/sh

if [ "$JAIL_MCP_HTTP" = "true" ]; then
    # wrapper that enables a stdio server to run in http
    echo "INFO: starting jail MCP in HTTP mode" >&2
    exec mcpo --port 8001 -- jail-mcp
else
    echo "INFO: \$JAIL_MCP_HTTP not set, starting jail MCP stdio" >&2
    exec jail-mcp
fi
